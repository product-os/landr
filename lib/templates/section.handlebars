<div id="{{ id }}" class="{{ class }}">
  <div class="container">
      {{> sectionHeader title=title lead=lead }}
    <div class="row col-md-8 offset-md-2">
      {{{ content }}}
      {{#each items}}
        <p>{{{ this }}}</p>
      {{/each}}
    </div>
  </div>
</div>
